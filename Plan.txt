pytesseract + Tesseract OCR (for scanned image pdfs)
    Things you'll probably want to handle:

    page ordering

    removing headers/footers

    splitting text into chunks (important for TTS)
Piper TTS (text to speech)

OpenAI TTS

Drag and drop
Tkinter + tkinterdnd2
get file path -> process and output pdf.

pyinstaller for .exe

pyDub
TTS output → WAV
WAV → MP3 (ffmpeg)

voice model
Download en_GB-cori-high.onnx
https://github.com/rhasspy/piper/blob/master/VOICES.md

place it in your local directory.
